// Re-export usePlayerStateMachine as useSourceFailover for backwards compatibility.
// Single source of truth playback state management.
export { usePlayerStateMachine as useSourceFailover } from "./usePlayerStateMachine";
